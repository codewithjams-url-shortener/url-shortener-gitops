apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-boot-service.fullname" . }}
  labels:
    {{- include "spring-boot-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-boot-service.selectorLabels" . | nindent 4 }}
