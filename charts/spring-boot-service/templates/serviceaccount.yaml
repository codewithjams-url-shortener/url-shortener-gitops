{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spring-boot-service.serviceAccountName" . }}
  labels:
    {{- include "spring-boot-service.labels" . | nindent 4 }}
{{- end }}
