apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redirect-service
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/codewithjams-url-shortener/url-shortener-gitops.git
      targetRevision: main
      path: charts/spring-boot-service
      helm:
        valueFiles:
          - $values/services/redirect-service/values-local.yaml
    - repoURL: https://github.com/codewithjams-url-shortener/url-shortener-gitops.git
      targetRevision: main
      path: services/redirect-service
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: redirect-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
